<h1>New Supplier</h1>

<%= render 'form', supplier: @supplier %>


<%= link_to 'Back', suppliers_path %>
<br>
<br>
<% if user_signed_in? && (current_user.has_role? :admin) %>
  <%= link_to 'Supplier Registration', admin_index_path %>
<% end %>
