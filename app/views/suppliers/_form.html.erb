<%= form_for(supplier) do |f| %>
  <% if supplier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supplier.errors.count, "error") %> prohibited this supplier from being saved:</h2>

      <ul>
      <% supplier.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :supplier_name %>
    <%= f.text_field :supplier_name %>
  </div>

  <div class="field">
    <%#= f.label :email %>
    <%#= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :supplier_phone_number %>
    <%= f.text_field :supplier_phone_number %>
  </div>

  <div class="field">
    <%= f.label :company_number %>
    <%= f.text_field :company_number %>
  </div>

  <% if user_signed_in? %>
     <% if current_user.has_role? :admin %>
       <div class="field">
         <%= f.label :verified %>
         <%= f.check_box :verified %>
       </div>
     <% else %>
     <% end %>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
